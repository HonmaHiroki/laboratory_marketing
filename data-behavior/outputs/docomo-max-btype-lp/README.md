# BタイプLP デザイン実装（比較検討・不安解消型）

添付の「広告訴求LP構成書（Bタイプ向け）」および `rank_lp_wireframe_abtest_design.xlsx` / `rank_lp_full_copy_draft.xlsx` のBランク行をもとに、
`_shared/skills/ad-lp-design.md` の設計思想に沿って実装したデザインカンプ（動作するHTML）です。

- 成果物：
  - `index.html` — キャンペーンLP（`B_campaign`）
  - `entertainment.html` — エンタメ特典LP（`B_entertainment`）
- いずれも単一ファイル・外部依存なし。相互にリンク済み（エンタメLPのキャンペーン接続 → キャンペーンLP）
- 作成日：2026-08-16
- 作成エージェント：campaign-executor（③ data-behavior）

## 実装済みの仕様｜キャンペーンLP（index.html）

| 構成書の指定 | 実装状況 |
|---|---|
| 13セクション構成（①FV〜⑬フッター） | 全セクション実装。`section-b-*` の命名規則も構成書 §3 に準拠 |
| 対象者チェックUI（WFシート 02_対象者チェック） | 3問のセルフ判定として実装。入力・送信なしのクライアント処理のみ |
| デザイントンマナ | メイン `#1F4E78` ／ サブ `#EAF3F8` ／ CTA `#D9002F` ／ 白基調 |
| CTA配置（最低4箇所） | FV・解決策提示後・社会的証明後・オファー・最終CTA・追従バーの6箇所 |
| モバイルファースト | SP基準で設計しPCへ拡張。SPのFVは1画面に収まる（390×844で検証済み） |
| 表の縦カード化 | プラン比較はSPで横スクロールさせず縦カード。PCで2カラム |
| 画像内テキストなし | 画像素材を使わずCSS/SVGで実装。文字はすべてHTMLテキストレイヤー |
| FAQ | 上位3問を初期表示、残り4問は開閉。原稿シート 04_FAQ原稿 の文言をそのまま使用 |
| 計測イベント | `lp_view` / `lp_campaign_fv_cta_click` / `eligibility_check_start` / `eligibility_complete` / `compare_module_view` / `faq_open` / `application_click` ほかを `dataLayer.push` で実装 |

## 実装済みの仕様｜エンタメ特典LP（entertainment.html）

WFシート `03_エンタメWF` のBランク行（8セクション）＋原稿シート `03_エンタメLP原稿` のP.S.を実装しています。

| 指定 | 実装状況 |
|---|---|
| 8セクション構成（01_FV〜08_クロージング）＋P.S. | 全セクション実装。命名は `section-bent-*` |
| ジャンル診断（6択） | スポーツ／バスケ・NBA／アニメ／映画・ドラマ／音楽／家族で視聴の6択＋「ギガ不安」導線。入力・送信なし |
| 回答による特典カードの出し分け | 選択ジャンルに応じて5枚のカードを **CSS order で並べ替え**、第一候補に「あなたにおすすめ」フラグを付与。追従CTAの文言も選択ジャンルに追随 |
| 特典カード5枚 | DAZN／NBA／dアニメストア／Lemino／統合（未確定者向け）。原稿シート `07_特典別モジュール原稿` の見出し・本文・CTAをそのまま使用 |
| SPは縦カード優先 | 横スクロールなし。SP1列／タブレット2列／PC3列 |
| Bランクのカード内CTA | 申込ではなく「詳細確認」型（`btn-sub`）。主導線はキャンペーンLPへの接続に一本化 |
| 利用シーン | 通勤・移動／家族視聴／試合観戦／月末のギガ不安の4シーン |
| 料金と特典価値のセット表示 | Bランク指定「料金と特典価値、既存契約注意点をセットで表示」に対応。注意事項アコーディオンと同一セクションに配置 |
| 計測イベント | `lp_view` / `lp_ent_fv_cta_click` / `genre_diagnosis_start` / `genre_diagnosis_complete` / `perk_card_click_{perk}` / `scene_module_view` / `ent_to_campaign_click` / `perk_notice_open` / `ent_faq_open` / `lp_ent_bottom_cta_click` / `ps_cta_click`。全イベントに `genre` パラメータが付与される |

FVヘッドラインは3案を切替できます（`?fv=default` / `?fv=a` / `?fv=b`）。既定はBランク原稿、A案・B案は構成書 §7「エンタメLP FV」のA案_統合／B案_ジャンル別です。

## ABテスト（構成書 §7 の優先1〜3）｜キャンペーンLP

レビュー用の切替パネル（右上「AB」ボタン）またはURLパラメータで比較できます。

| 優先 | テスト対象 | パラメータ |
|---|---|---|
| 1 | FVヘッドライン A案／B案 | `?h=a` / `?h=b` |
| 2 | FV CTA文言 A案／B案 | `?c=a` / `?c=b` |
| 3 | FAQ配置（中盤後半／FV直下） | `?f=a` / `?f=b` |

例：`index.html?h=b&c=b&f=b`

計測イベントには `lp_variant`（例：`hb_cb_fb`）が付与されるため、そのままバリアント別の集計に使えます。
本番実装ではこのパネルを削除し、LPツール／GTM側で出し分けてください。

## 公開前に必ず対応が必要な項目

1. **モックアップ表示の削除**：最上部の帯、右上のABパネル（`.mock-banner` / `.rev` / `.rev-toggle`）、`<meta name="robots" content="noindex, nofollow">`
2. **法務・公式確認**：黄色い網掛け（`.verify`）を付けた数値・条件がすべて対象です（`08_表記確認メモ` に準拠）
   - 3,000ポイントの付与条件・進呈時期・有効期限・対象外条件
   - 対象プランの最新の範囲
   - プラン料金（8,448円／5,148円／11,748円／2,948円）、還元率10%、上限5,000pt、選べる特典の対象サービス数
3. **リンク先の設定**：`href="#"` のCTA・フッターリンクを実URLに差し替え
4. **UTM／計測タグ**：`utm_source={media}&utm_medium=paid&utm_campaign=max_btype&utm_content={creative}_{variant}` をGTM側で設定

## 構成書からの意図的な差分

- 構成書 §3 のセクション順（FAQ＝⑩）を既定とし、WFシートBランク行の「FAQをFV直下」はABテストのB案として実装しました。両立させるための判断です。
- 「対象者チェック」は構成書の13セクションには明記がありませんが、WFシートのBランク順序（FV→対象者チェック→…）と基本方針「FAQと対象者チェックを上部に置く」に従い、④解決策提示の直後に配置しています。
- エンタメLPのFV見出しは、Bランク原稿（`03_エンタメLP原稿`）とABテスト差分（`06_ABテスト差分コピー`）で別の文言が指定されています。**Bランク原稿を既定**とし、ABテスト2案は切替候補として保持しました。
- エンタメLPのP.S.はWFシートの8セクションには含まれていませんが、原稿シートに存在するため、制作メモ「注意事項導線を近くに配置」に従いクロージング直後に配置しています。
